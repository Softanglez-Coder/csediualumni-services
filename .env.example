# Server Configuration
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/csediualumni
# For MongoDB Atlas (cloud): mongodb+srv://username:password@cluster.mongodb.net/csediualumni?retryWrites=true&w=majority

# Auth0 Configuration
# Get these values from your Auth0 dashboard: https://manage.auth0.com/
# 1. Domain: Found in your Auth0 Application settings (e.g., https://yourapp.us.auth0.com/)
# 2. Audience: The identifier of your Auth0 API (create one in APIs section if you haven't)
#    For multiple frontends using the SAME API, use one audience
#    For multiple audiences (less common), separate with commas: aud1,aud2,aud3
AUTH0_DOMAIN=https://csediualumni.us.auth0.com/
AUTH0_AUDIENCE=https://api.csedialumni.com

# Frontend Application Client IDs (one per frontend app)
# Get from Auth0 Dashboard → Applications → [Your App] → Settings
AUTH0_CLIENT_ID=your-web-app-client-id

ALLOWED_ORIGINS=https://web.csedialumni.com,https://admin.csedialumni.com